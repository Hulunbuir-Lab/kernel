#include <lwp.h>

// ELFProgram::ELFProgram() {
//     u8 *elfFileAddress = elfFile.readFile();
//     if (*((u32*) elfFileAddress) != 0x7F) {
//         uPut << "Not a ELF program\n";
//     }
//     if (*(elfFileAddress + 4) != 2) uPut << "This kernel may not support 32 bit elf program\n";
//     if (*(elfFileAddress + 5) != 1) uPut << "LoongArch only support little endian\n";
//     if (*(elfFileAddress + ))
// }
